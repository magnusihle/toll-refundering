# Local-only paths not needed by the Vercel build (web/ + api/ + package.json).
# Leading slash anchors each to the repo ROOT — WITHOUT it, `src/` would also match
# web/src/ and delete the frontend source (breaks the build).
/src/
/vendor/
/docs/
/test/
/scripts/
/.session/
/data/
*.log
